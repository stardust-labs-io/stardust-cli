#!/usr/bin/env bash
#
# stardust - Stardust Jams CLI
# Auto-generated from OpenAPI spec v0.1.0
# Usage: stardust <command> [options]
#
# Authentication: Set STARDUST_API_KEY environment variable
#   export STARDUST_API_KEY=sj_your_key_here
#
# Server: Set STARDUST_API_URL (default: https://stardust-jams.stardustlabs.io)
#   export STARDUST_API_URL=http://localhost:5550
#

set -euo pipefail

API_URL="${STARDUST_API_URL:-https://stardust-jams.stardustlabs.io}"
API_KEY="${STARDUST_API_KEY:-}"

if [[ -z "$API_KEY" ]]; then
    echo "Error: STARDUST_API_KEY not set. Create one in the app under Settings > API Keys." >&2
    exit 1
fi

# --- Helpers ---

api_request() {
    local method="$1"
    local path="$2"
    shift 2

    local url="${API_URL}${path}"
    local args=(-s -S -X "$method" -H "X-API-Key: $API_KEY" -H "Content-Type: application/json")

    for arg in "$@"; do
        args+=("$arg")
    done

    local response
    local http_code
    local body
    response=$(curl "${args[@]}" -w "\n%{http_code}" "$url")
    http_code=$(echo "$response" | tail -1)
    body=$(echo "$response" | sed '$d')

    if [[ "$http_code" -ge 400 ]]; then
        echo "Error ($http_code): $body" >&2
        return 1
    fi

    echo "$body"
}

pretty() {
    if command -v jq &>/dev/null; then
        jq .
    else
        cat
    fi
}

# --- Commands ---

create_artist() {
    local body="${1:-{}}"
    api_request POST "/artists" -d "$body" | pretty
}

get_artist() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_artist_events() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

request_membership() {
    local artist_id="${1:?"artist_id is required"}"
    api_request POST "/artists/$artist_id/members" | pretty
}

remove_own_membership() {
    local artist_id="${1:?"artist_id is required"}"
    api_request DELETE "/artists/$artist_id/members/me" | pretty
}

approve_membership() {
    local artist_id="${1:?"artist_id is required"}"
    local member_user_id="${2:?"member_user_id is required"}"
    api_request POST "/artists/$artist_id/members/$member_user_id/approve" | pretty
}

reject_membership() {
    local artist_id="${1:?"artist_id is required"}"
    local member_user_id="${2:?"member_user_id is required"}"
    api_request POST "/artists/$artist_id/members/$member_user_id/reject" | pretty
}

get_artist_notes() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id/notes"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_artist_recordings() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id/recordings"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

auth_apple() {
    local body="${1:-{}}"
    api_request POST "/auth/apple" -d "$body" | pretty
}

auth_apple_refresh() {
    local body="${1:-{}}"
    api_request POST "/auth/apple/refresh" -d "$body" | pretty
}

auth_refresh() {
    local body="${1:-{}}"
    api_request POST "/auth/refresh" -d "$body" | pretty
}

auth_sign_out() {
    local body="${1:-{}}"
    api_request POST "/auth/sign-out" -d "$body" | pretty
}

get_content_url() {
    local content_type="${1:?"content_type is required"}"
    local content_id="${2:?"content_id is required"}"
    local query="${3:-}"
    local path="/content/$content_type/$content_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_events_by_id() {
    local query="${1:-}"
    local path="/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_event() {
    local body="${1:-{}}"
    api_request POST "/events" -d "$body" | pretty
}

check_event_statuses() {
    local body="${1:-{}}"
    api_request POST "/events/sync" -d "$body" | pretty
}

get_event_by_uuid() {
    local uuid="${1:?"uuid is required"}"
    local query="${2:-}"
    local path="/events/uuid/$uuid"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

post_event_users() {
    local code="${1:?"code is required"}"
    local query="${2:-}"
    local path="/events/$code/users"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_event() {
    local event_id="${1:?"event_id is required"}"
    local query="${2:-}"
    local path="/events/$event_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

update_event() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/events/$event_id" -d "$body" | pretty
}

like_event() {
    local event_id="${1:?"event_id is required"}"
    api_request POST "/events/$event_id/like" | pretty
}

set_rsvp_status() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request POST "/events/$event_id/rsvp" -d "$body" | pretty
}

get_set_list() {
    local event_id="${1:?"event_id is required"}"
    local query="${2:-}"
    local path="/events/$event_id/setlist"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

add_to_set_list() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request POST "/events/$event_id/setlist" -d "$body" | pretty
}

reorder_set_list() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/events/$event_id/setlist/reorder" -d "$body" | pretty
}

remove_from_set_list() {
    local event_id="${1:?"event_id is required"}"
    local note_id="${2:?"note_id is required"}"
    api_request DELETE "/events/$event_id/setlist/$note_id" | pretty
}

add_event_songs() {
    local event_id="${1:?"event_id is required"}"
    local query="${2:-}"
    local path="/events/$event_id/songs"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

unlike_event() {
    local event_id="${1:?"event_id is required"}"
    api_request POST "/events/$event_id/unlike" | pretty
}

get_locations() {
    local query="${1:-}"
    local path="/locations"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_location() {
    local location_id="${1:?"location_id is required"}"
    local query="${2:-}"
    local path="/locations/$location_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_map_events() {
    local query="${1:-}"
    local path="/map/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_note_tags() {
    local query="${1:-}"
    local path="/note-tags"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_note_tag() {
    local body="${1:-{}}"
    api_request POST "/note-tags" -d "$body" | pretty
}

delete_note_tag() {
    local note_tag_id="${1:?"note_tag_id is required"}"
    api_request DELETE "/note-tags/$note_tag_id" | pretty
}

update_note_tag() {
    local note_tag_id="${1:?"note_tag_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/note-tags/$note_tag_id" -d "$body" | pretty
}

get_notes_by_id() {
    local query="${1:-}"
    local path="/notes"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

upload_note() {
    local body="${1:-{}}"
    api_request POST "/notes" -d "$body" | pretty
}

get_notes_by_note_tag() {
    local note_tag_id="${1:?"note_tag_id is required"}"
    local query="${2:-}"
    local path="/notes/by-note-tag/$note_tag_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_note_collections_by_id() {
    local query="${1:-}"
    local path="/notes/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_suggested_tips() {
    local body="${1:-{}}"
    api_request POST "/notes/suggested-tips" -d "$body" | pretty
}

check_note_statuses() {
    local body="${1:-{}}"
    api_request POST "/notes/sync" -d "$body" | pretty
}

get_note_by_uuid() {
    local uuid="${1:?"uuid is required"}"
    local query="${2:-}"
    local path="/notes/uuid/$uuid"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_tags_for_note() {
    local note_id="${1:?"note_id is required"}"
    local query="${2:-}"
    local path="/notes/$note_id/note-tags"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

set_tags_for_note() {
    local note_id="${1:?"note_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/notes/$note_id/note-tags" -d "$body" | pretty
}

remove_tag_from_note() {
    local note_id="${1:?"note_id is required"}"
    local note_tag_id="${2:?"note_tag_id is required"}"
    api_request DELETE "/notes/$note_id/note-tags/$note_tag_id" | pretty
}

create_post() {
    local body="${1:-{}}"
    api_request POST "/posts" -d "$body" | pretty
}

like_comment() {
    local comment_id="${1:?"comment_id is required"}"
    api_request POST "/posts/comments/$comment_id/like" | pretty
}

unlike_comment() {
    local comment_id="${1:?"comment_id is required"}"
    api_request POST "/posts/comments/$comment_id/unlike" | pretty
}

get_post_comments() {
    local post_id="${1:?"post_id is required"}"
    local query="${2:-}"
    local path="/posts/$post_id/comments"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_comment() {
    local post_id="${1:?"post_id is required"}"
    local body="${2:-{}}"
    api_request POST "/posts/$post_id/comments" -d "$body" | pretty
}

like_post() {
    local post_id="${1:?"post_id is required"}"
    api_request POST "/posts/$post_id/like" | pretty
}

unlike_post() {
    local post_id="${1:?"post_id is required"}"
    api_request POST "/posts/$post_id/unlike" | pretty
}

get_recordings_by_id() {
    local query="${1:-}"
    local path="/recordings"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

upload_recording() {
    local body="${1:-{}}"
    api_request POST "/recordings" -d "$body" | pretty
}

get_recording_collections_by_id() {
    local query="${1:-}"
    local path="/recordings/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

check_recording_statuses() {
    local body="${1:-{}}"
    api_request POST "/recordings/sync" -d "$body" | pretty
}

get_recording_by_uuid() {
    local uuid="${1:?"uuid is required"}"
    local query="${2:-}"
    local path="/recordings/uuid/$uuid"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_recording() {
    local recording_id="${1:?"recording_id is required"}"
    local query="${2:-}"
    local path="/recordings/$recording_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

like_recording() {
    local recording_id="${1:?"recording_id is required"}"
    api_request POST "/recordings/$recording_id/like" | pretty
}

unlike_recording() {
    local recording_id="${1:?"recording_id is required"}"
    api_request POST "/recordings/$recording_id/unlike" | pretty
}

get_search() {
    local query="${1:-}"
    local path="/search"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_songs_by_id() {
    local query="${1:-}"
    local path="/songs"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_songs_by_spotify_id() {
    local body="${1:-{}}"
    api_request GET "/songs/spotify" -d "$body" | pretty
}

get_users() {
    local query="${1:-}"
    local path="/users"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_test_user() {
    api_request POST "/users" | pretty
}

list_api_keys() {
    local query="${1:-}"
    local path="/users/me/api-keys"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_api_key() {
    local body="${1:-{}}"
    api_request POST "/users/me/api-keys" -d "$body" | pretty
}

delete_api_key() {
    local api_key_id="${1:?"api_key_id is required"}"
    api_request DELETE "/users/me/api-keys/$api_key_id" | pretty
}

get_notifications() {
    local query="${1:-}"
    local path="/users/me/notifications"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_preferences() {
    local query="${1:-}"
    local path="/users/me/preferences"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

update_preferences() {
    local body="${1:-{}}"
    api_request PUT "/users/me/preferences" -d "$body" | pretty
}

clear_primary_artist() {
    api_request DELETE "/users/me/primary-artist" | pretty
}

set_primary_artist() {
    local body="${1:-{}}"
    api_request PUT "/users/me/primary-artist" -d "$body" | pretty
}

get_user_note_collections() {
    local created_by_id="${1:?"created_by_id is required"}"
    local query="${2:-}"
    local path="/users/$created_by_id/notes/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_recording_collections() {
    local created_by_id="${1:?"created_by_id is required"}"
    local query="${2:-}"
    local path="/users/$created_by_id/recordings/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

add_user_link() {
    local logged_in_user_id="${1:?"logged_in_user_id is required"}"
    api_request POST "/users/$logged_in_user_id/user_links" | pretty
}

delete_user_link() {
    local logged_in_user_id="${1:?"logged_in_user_id is required"}"
    local link_to_delete_id="${2:?"link_to_delete_id is required"}"
    api_request DELETE "/users/$logged_in_user_id/user_links/$link_to_delete_id" | pretty
}

get_user() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_events() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_notes() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id/notes"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_recordings() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id/recordings"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

update_user() {
    local user_to_change_id="${1:?"user_to_change_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/users/$user_to_change_id" -d "$body" | pretty
}

get_profile_image_upload_url() {
    local user_to_change_id="${1:?"user_to_change_id is required"}"
    api_request POST "/users/$user_to_change_id/image/upload-url" | pretty
}

create_venue() {
    local body="${1:-{}}"
    api_request POST "/venues" -d "$body" | pretty
}

get_venue() {
    local venue_id="${1:?"venue_id is required"}"
    local query="${2:-}"
    local path="/venues/$venue_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_venue_events() {
    local venue_id="${1:?"venue_id is required"}"
    local query="${2:-}"
    local path="/venues/$venue_id/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

show_help() {
    cat <<HELP
stardust - Stardust Jams CLI

Usage: stardust <command> [args...]

Commands:
  create-artist [json-body]                        Create Artist
  get-artist <artist_id>                           Get Artist
  get-artist-events <artist_id>                    Get Artist Events
  request-membership <artist_id>                   Request Membership
  remove-own-membership <artist_id>                Remove Own Membership
  approve-membership <artist_id> <member_user_id>  Approve Membership
  reject-membership <artist_id> <member_user_id>   Reject Membership
  get-artist-notes <artist_id>                     Get Artist Notes
  get-artist-recordings <artist_id>                Get Artist Recordings
  auth-apple [json-body]                           Auth Apple
  auth-apple-refresh [json-body]                   Auth Refresh
  auth-refresh [json-body]                         Auth Refresh
  auth-sign-out [json-body]                        Auth Sign Out
  get-content-url <content_type> <content_id>      Get Content Url
  get-events-by-id                                 Get Events By Id
  create-event [json-body]                         Create Event
  check-event-statuses [json-body]                 Check Event Statuses
  get-event-by-uuid <uuid>                         Get Event By Uuid
  post-event-users <code>                          Post Event Users
  get-event <event_id>                             Get Event
  update-event <event_id> [json-body]              Update Event
  like-event <event_id>                            Like Event
  set-rsvp-status <event_id> [json-body]           Set Rsvp Status
  get-set-list <event_id>                          Get Set List
  add-to-set-list <event_id> [json-body]           Add To Set List
  reorder-set-list <event_id> [json-body]          Reorder Set List
  remove-from-set-list <event_id> <note_id>        Remove From Set List
  add-event-songs <event_id>                       Add Event Songs
  unlike-event <event_id>                          Unlike Event
  get-locations                                    Get Locations
  get-location <location_id>                       Get Location
  get-map-events                                   Get Map Events
  get-note-tags                                    Get Note Tags
  create-note-tag [json-body]                      Create Note Tag
  delete-note-tag <note_tag_id>                    Delete Note Tag
  update-note-tag <note_tag_id> [json-body]        Update Note Tag
  get-notes-by-id                                  Get Notes By Id
  upload-note [json-body]                          Upload Note
  get-notes-by-note-tag <note_tag_id>              Get Notes By Note Tag
  get-note-collections-by-id                       Get Note Collections By Id
  get-suggested-tips [json-body]                   Get Suggested Tips
  check-note-statuses [json-body]                  Check Note Statuses
  get-note-by-uuid <uuid>                          Get Note By Uuid
  get-tags-for-note <note_id>                      Get Tags For Note
  set-tags-for-note <note_id> [json-body]          Set Tags For Note
  remove-tag-from-note <note_id> <note_tag_id>     Remove Tag From Note
  create-post [json-body]                          Create Post
  like-comment <comment_id>                        Like Comment
  unlike-comment <comment_id>                      Unlike Comment
  get-post-comments <post_id>                      Get Post Comments
  create-comment <post_id> [json-body]             Create Comment
  like-post <post_id>                              Like Post
  unlike-post <post_id>                            Unlike Post
  get-recordings-by-id                             Get Recordings By Id
  upload-recording [json-body]                     Upload Recording
  get-recording-collections-by-id                  Get Recording Collections By Id
  check-recording-statuses [json-body]             Check Recording Statuses
  get-recording-by-uuid <uuid>                     Get Recording By Uuid
  get-recording <recording_id>                     Get Recording
  like-recording <recording_id>                    Like Recording
  unlike-recording <recording_id>                  Unlike Recording
  get-search                                       Get Search
  get-songs-by-id                                  Get Songs By Id
  get-songs-by-spotify-id [json-body]              Get Songs By Spotify Id
  get-users                                        Get Users
  create-test-user                                 Create Test User
  list-api-keys                                    List Api Keys
  create-api-key [json-body]                       Create Api Key
  delete-api-key <api_key_id>                      Delete Api Key
  get-notifications                                Get Notifications
  get-preferences                                  Get Preferences
  update-preferences [json-body]                   Update Preferences
  clear-primary-artist                             Clear Primary Artist
  set-primary-artist [json-body]                   Set Primary Artist
  get-user-note-collections <created_by_id>        Get User Note Collections
  get-user-recording-collections <created_by_id>   Get User Recording Collections
  add-user-link <logged_in_user_id>                Add User Link
  delete-user-link <logged_in_user_id> <link_to_delete_id>  Delete User Link
  get-user <user_id>                               Get User
  get-user-events <user_id>                        Get User Events
  get-user-notes <user_id>                         Get User Notes
  get-user-recordings <user_id>                    Get User Recordings
  update-user <user_to_change_id> [json-body]      Update User
  get-profile-image-upload-url <user_to_change_id>  Get Profile Image Upload Url
  create-venue [json-body]                         Create Venue
  get-venue <venue_id>                             Get Venue
  get-venue-events <venue_id>                      Get Venue Events

Environment:
  STARDUST_API_KEY    API key for authentication (required)
  STARDUST_API_URL    API server URL (default: https://stardust-jams.stardustlabs.io)
HELP
}

command="${1:-help}"
shift || true

case "$command" in
    create-artist) create_artist "$@" ;;
    get-artist) get_artist "$@" ;;
    get-artist-events) get_artist_events "$@" ;;
    request-membership) request_membership "$@" ;;
    remove-own-membership) remove_own_membership "$@" ;;
    approve-membership) approve_membership "$@" ;;
    reject-membership) reject_membership "$@" ;;
    get-artist-notes) get_artist_notes "$@" ;;
    get-artist-recordings) get_artist_recordings "$@" ;;
    auth-apple) auth_apple "$@" ;;
    auth-apple-refresh) auth_apple_refresh "$@" ;;
    auth-refresh) auth_refresh "$@" ;;
    auth-sign-out) auth_sign_out "$@" ;;
    get-content-url) get_content_url "$@" ;;
    get-events-by-id) get_events_by_id "$@" ;;
    create-event) create_event "$@" ;;
    check-event-statuses) check_event_statuses "$@" ;;
    get-event-by-uuid) get_event_by_uuid "$@" ;;
    post-event-users) post_event_users "$@" ;;
    get-event) get_event "$@" ;;
    update-event) update_event "$@" ;;
    like-event) like_event "$@" ;;
    set-rsvp-status) set_rsvp_status "$@" ;;
    get-set-list) get_set_list "$@" ;;
    add-to-set-list) add_to_set_list "$@" ;;
    reorder-set-list) reorder_set_list "$@" ;;
    remove-from-set-list) remove_from_set_list "$@" ;;
    add-event-songs) add_event_songs "$@" ;;
    unlike-event) unlike_event "$@" ;;
    get-locations) get_locations "$@" ;;
    get-location) get_location "$@" ;;
    get-map-events) get_map_events "$@" ;;
    get-note-tags) get_note_tags "$@" ;;
    create-note-tag) create_note_tag "$@" ;;
    delete-note-tag) delete_note_tag "$@" ;;
    update-note-tag) update_note_tag "$@" ;;
    get-notes-by-id) get_notes_by_id "$@" ;;
    upload-note) upload_note "$@" ;;
    get-notes-by-note-tag) get_notes_by_note_tag "$@" ;;
    get-note-collections-by-id) get_note_collections_by_id "$@" ;;
    get-suggested-tips) get_suggested_tips "$@" ;;
    check-note-statuses) check_note_statuses "$@" ;;
    get-note-by-uuid) get_note_by_uuid "$@" ;;
    get-tags-for-note) get_tags_for_note "$@" ;;
    set-tags-for-note) set_tags_for_note "$@" ;;
    remove-tag-from-note) remove_tag_from_note "$@" ;;
    create-post) create_post "$@" ;;
    like-comment) like_comment "$@" ;;
    unlike-comment) unlike_comment "$@" ;;
    get-post-comments) get_post_comments "$@" ;;
    create-comment) create_comment "$@" ;;
    like-post) like_post "$@" ;;
    unlike-post) unlike_post "$@" ;;
    get-recordings-by-id) get_recordings_by_id "$@" ;;
    upload-recording) upload_recording "$@" ;;
    get-recording-collections-by-id) get_recording_collections_by_id "$@" ;;
    check-recording-statuses) check_recording_statuses "$@" ;;
    get-recording-by-uuid) get_recording_by_uuid "$@" ;;
    get-recording) get_recording "$@" ;;
    like-recording) like_recording "$@" ;;
    unlike-recording) unlike_recording "$@" ;;
    get-search) get_search "$@" ;;
    get-songs-by-id) get_songs_by_id "$@" ;;
    get-songs-by-spotify-id) get_songs_by_spotify_id "$@" ;;
    get-users) get_users "$@" ;;
    create-test-user) create_test_user "$@" ;;
    list-api-keys) list_api_keys "$@" ;;
    create-api-key) create_api_key "$@" ;;
    delete-api-key) delete_api_key "$@" ;;
    get-notifications) get_notifications "$@" ;;
    get-preferences) get_preferences "$@" ;;
    update-preferences) update_preferences "$@" ;;
    clear-primary-artist) clear_primary_artist "$@" ;;
    set-primary-artist) set_primary_artist "$@" ;;
    get-user-note-collections) get_user_note_collections "$@" ;;
    get-user-recording-collections) get_user_recording_collections "$@" ;;
    add-user-link) add_user_link "$@" ;;
    delete-user-link) delete_user_link "$@" ;;
    get-user) get_user "$@" ;;
    get-user-events) get_user_events "$@" ;;
    get-user-notes) get_user_notes "$@" ;;
    get-user-recordings) get_user_recordings "$@" ;;
    update-user) update_user "$@" ;;
    get-profile-image-upload-url) get_profile_image_upload_url "$@" ;;
    create-venue) create_venue "$@" ;;
    get-venue) get_venue "$@" ;;
    get-venue-events) get_venue_events "$@" ;;
    help|--help|-h) show_help ;;
    *)
        echo "Unknown command: $command" >&2
        echo "Run 'stardust help' for usage." >&2
        exit 1
        ;;
esac
