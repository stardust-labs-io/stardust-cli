#!/usr/bin/env bash
#
# stardust - Stardust Jams CLI
# Auto-generated from OpenAPI spec v0.1.0
# Usage: stardust <command> [options]
#
# Authentication: Set STARDUST_API_KEY environment variable
#   export STARDUST_API_KEY=sj_your_key_here
#
# Server: Set STARDUST_API_URL (default: https://stardust-jams.stardustlabs.io)
#   export STARDUST_API_URL=http://localhost:5550
#

set -euo pipefail

API_URL="${STARDUST_API_URL:-https://stardust-jams.stardustlabs.io}"
API_KEY="${STARDUST_API_KEY:-}"

if [[ -z "$API_KEY" ]]; then
    echo "Error: STARDUST_API_KEY not set. Create one in the app under Settings > API Keys." >&2
    exit 1
fi

# --- Helpers ---

api_request() {
    local method="$1"
    local path="$2"
    shift 2

    local url="${API_URL}${path}"
    local args=(-s -S -X "$method" -H "X-API-Key: $API_KEY" -H "Content-Type: application/json")

    for arg in "$@"; do
        args+=("$arg")
    done

    local response
    local http_code
    response=$(curl "${args[@]}" -w "\n%{http_code}" "$url")
    http_code=$(echo "$response" | tail -1)
    local body
    body=$(echo "$response" | sed '$d')

    if [[ "$http_code" -ge 400 ]]; then
        echo "Error ($http_code): $body" >&2
        return 1
    fi

    echo "$body"
}

pretty() {
    if command -v jq &>/dev/null; then
        jq .
    else
        cat
    fi
}

# --- Commands ---

get_status_get() {
    local query="${1:-}"
    local path="/"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_status_status_get() {
    local query="${1:-}"
    local path="/_status"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_artist_artists_post() {
    local body="${1:-{}}"
    api_request POST "/artists" -d "$body" | pretty
}

get_artist_artists_artist_id_get() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_artist_events_artists_artist_id_events_get() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

request_membership_artists_artist_id_members_post() {
    local artist_id="${1:?"artist_id is required"}"
    api_request POST "/artists/$artist_id/members" | pretty
}

remove_own_membership_artists_artist_id_members_me_delete() {
    local artist_id="${1:?"artist_id is required"}"
    api_request DELETE "/artists/$artist_id/members/me" | pretty
}

approve_membership_artists_artist_id_members_member_user_id_approve_post() {
    local artist_id="${1:?"artist_id is required"}"
    local member_user_id="${2:?"member_user_id is required"}"
    api_request POST "/artists/$artist_id/members/$member_user_id/approve" | pretty
}

reject_membership_artists_artist_id_members_member_user_id_reject_post() {
    local artist_id="${1:?"artist_id is required"}"
    local member_user_id="${2:?"member_user_id is required"}"
    api_request POST "/artists/$artist_id/members/$member_user_id/reject" | pretty
}

get_artist_notes_artists_artist_id_notes_get() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id/notes"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_artist_recordings_artists_artist_id_recordings_get() {
    local artist_id="${1:?"artist_id is required"}"
    local query="${2:-}"
    local path="/artists/$artist_id/recordings"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

auth_apple_auth_apple_post() {
    local body="${1:-{}}"
    api_request POST "/auth/apple" -d "$body" | pretty
}

auth_refresh_auth_apple_refresh_post() {
    local body="${1:-{}}"
    api_request POST "/auth/apple/refresh" -d "$body" | pretty
}

auth_refresh_auth_refresh_post() {
    local body="${1:-{}}"
    api_request POST "/auth/refresh" -d "$body" | pretty
}

auth_sign_out_auth_sign_out_post() {
    local body="${1:-{}}"
    api_request POST "/auth/sign-out" -d "$body" | pretty
}

get_content_url_content_content_type_content_id_get() {
    local content_type="${1:?"content_type is required"}"
    local content_id="${2:?"content_id is required"}"
    local query="${3:-}"
    local path="/content/$content_type/$content_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_events_by_id_events_get() {
    local query="${1:-}"
    local path="/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_event_events_post() {
    local body="${1:-{}}"
    api_request POST "/events" -d "$body" | pretty
}

check_event_statuses_events_sync_post() {
    local body="${1:-{}}"
    api_request POST "/events/sync" -d "$body" | pretty
}

get_event_by_uuid_events_uuid_uuid_get() {
    local uuid="${1:?"uuid is required"}"
    local query="${2:-}"
    local path="/events/uuid/$uuid"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

post_event_users_events_code_users_get() {
    local code="${1:?"code is required"}"
    local query="${2:-}"
    local path="/events/$code/users"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_event_events_event_id_get() {
    local event_id="${1:?"event_id is required"}"
    local query="${2:-}"
    local path="/events/$event_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

update_event_events_event_id_put() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/events/$event_id" -d "$body" | pretty
}

like_event_events_event_id_like_post() {
    local event_id="${1:?"event_id is required"}"
    api_request POST "/events/$event_id/like" | pretty
}

set_rsvp_status_events_event_id_rsvp_post() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request POST "/events/$event_id/rsvp" -d "$body" | pretty
}

get_set_list_events_event_id_setlist_get() {
    local event_id="${1:?"event_id is required"}"
    local query="${2:-}"
    local path="/events/$event_id/setlist"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

add_to_set_list_events_event_id_setlist_post() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request POST "/events/$event_id/setlist" -d "$body" | pretty
}

reorder_set_list_events_event_id_setlist_reorder_put() {
    local event_id="${1:?"event_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/events/$event_id/setlist/reorder" -d "$body" | pretty
}

remove_from_set_list_events_event_id_setlist_note_id_delete() {
    local event_id="${1:?"event_id is required"}"
    local note_id="${2:?"note_id is required"}"
    api_request DELETE "/events/$event_id/setlist/$note_id" | pretty
}

add_event_songs_events_event_id_songs_get() {
    local event_id="${1:?"event_id is required"}"
    local query="${2:-}"
    local path="/events/$event_id/songs"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

unlike_event_events_event_id_unlike_post() {
    local event_id="${1:?"event_id is required"}"
    api_request POST "/events/$event_id/unlike" | pretty
}

get_locations_locations_get() {
    local query="${1:-}"
    local path="/locations"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_location_locations_location_id_get() {
    local location_id="${1:?"location_id is required"}"
    local query="${2:-}"
    local path="/locations/$location_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_map_events_map_events_get() {
    local query="${1:-}"
    local path="/map/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_note_tags_note_tags_get() {
    local query="${1:-}"
    local path="/note-tags"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_note_tag_note_tags_post() {
    local body="${1:-{}}"
    api_request POST "/note-tags" -d "$body" | pretty
}

delete_note_tag_note_tags_note_tag_id_delete() {
    local note_tag_id="${1:?"note_tag_id is required"}"
    api_request DELETE "/note-tags/$note_tag_id" | pretty
}

update_note_tag_note_tags_note_tag_id_put() {
    local note_tag_id="${1:?"note_tag_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/note-tags/$note_tag_id" -d "$body" | pretty
}

get_notes_by_id_notes_get() {
    local query="${1:-}"
    local path="/notes"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

upload_note_notes_post() {
    local body="${1:-{}}"
    api_request POST "/notes" -d "$body" | pretty
}

get_notes_by_note_tag_notes_by_note_tag_note_tag_id_get() {
    local note_tag_id="${1:?"note_tag_id is required"}"
    local query="${2:-}"
    local path="/notes/by-note-tag/$note_tag_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_note_collections_by_id_notes_collections_get() {
    local query="${1:-}"
    local path="/notes/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_suggested_tips_notes_suggested_tips_post() {
    local body="${1:-{}}"
    api_request POST "/notes/suggested-tips" -d "$body" | pretty
}

check_note_statuses_notes_sync_post() {
    local body="${1:-{}}"
    api_request POST "/notes/sync" -d "$body" | pretty
}

get_note_by_uuid_notes_uuid_uuid_get() {
    local uuid="${1:?"uuid is required"}"
    local query="${2:-}"
    local path="/notes/uuid/$uuid"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_tags_for_note_notes_note_id_note_tags_get() {
    local note_id="${1:?"note_id is required"}"
    local query="${2:-}"
    local path="/notes/$note_id/note-tags"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

set_tags_for_note_notes_note_id_note_tags_put() {
    local note_id="${1:?"note_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/notes/$note_id/note-tags" -d "$body" | pretty
}

remove_tag_from_note_notes_note_id_note_tags_note_tag_id_delete() {
    local note_id="${1:?"note_id is required"}"
    local note_tag_id="${2:?"note_tag_id is required"}"
    api_request DELETE "/notes/$note_id/note-tags/$note_tag_id" | pretty
}

create_post_posts_post() {
    local body="${1:-{}}"
    api_request POST "/posts" -d "$body" | pretty
}

like_comment_posts_comments_comment_id_like_post() {
    local comment_id="${1:?"comment_id is required"}"
    api_request POST "/posts/comments/$comment_id/like" | pretty
}

unlike_comment_posts_comments_comment_id_unlike_post() {
    local comment_id="${1:?"comment_id is required"}"
    api_request POST "/posts/comments/$comment_id/unlike" | pretty
}

get_post_comments_posts_post_id_comments_get() {
    local post_id="${1:?"post_id is required"}"
    local query="${2:-}"
    local path="/posts/$post_id/comments"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_comment_posts_post_id_comments_post() {
    local post_id="${1:?"post_id is required"}"
    local body="${2:-{}}"
    api_request POST "/posts/$post_id/comments" -d "$body" | pretty
}

like_post_posts_post_id_like_post() {
    local post_id="${1:?"post_id is required"}"
    api_request POST "/posts/$post_id/like" | pretty
}

unlike_post_posts_post_id_unlike_post() {
    local post_id="${1:?"post_id is required"}"
    api_request POST "/posts/$post_id/unlike" | pretty
}

get_purposeful_debug_log_purposeful_debug_log_get() {
    local query="${1:-}"
    local path="/purposeful-debug-log"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_purposeful_error_purposeful_error_get() {
    local query="${1:-}"
    local path="/purposeful-error"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_purposeful_error_log_purposeful_error_log_get() {
    local query="${1:-}"
    local path="/purposeful-error-log"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_purposeful_info_log_purposeful_info_log_get() {
    local query="${1:-}"
    local path="/purposeful-info-log"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_purposeful_warning_log_purposeful_warning_log_get() {
    local query="${1:-}"
    local path="/purposeful-warning-log"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_recordings_by_id_recordings_get() {
    local query="${1:-}"
    local path="/recordings"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

upload_recording_recordings_post() {
    local body="${1:-{}}"
    api_request POST "/recordings" -d "$body" | pretty
}

get_recording_collections_by_id_recordings_collections_get() {
    local query="${1:-}"
    local path="/recordings/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

check_recording_statuses_recordings_sync_post() {
    local body="${1:-{}}"
    api_request POST "/recordings/sync" -d "$body" | pretty
}

get_recording_by_uuid_recordings_uuid_uuid_get() {
    local uuid="${1:?"uuid is required"}"
    local query="${2:-}"
    local path="/recordings/uuid/$uuid"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_recording_recordings_recording_id_get() {
    local recording_id="${1:?"recording_id is required"}"
    local query="${2:-}"
    local path="/recordings/$recording_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

like_recording_recordings_recording_id_like_post() {
    local recording_id="${1:?"recording_id is required"}"
    api_request POST "/recordings/$recording_id/like" | pretty
}

unlike_recording_recordings_recording_id_unlike_post() {
    local recording_id="${1:?"recording_id is required"}"
    api_request POST "/recordings/$recording_id/unlike" | pretty
}

register_device_register_device_post() {
    local body="${1:-{}}"
    api_request POST "/register_device" -d "$body" | pretty
}

get_search_search_get() {
    local query="${1:-}"
    local path="/search"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_songs_by_id_songs_get() {
    local query="${1:-}"
    local path="/songs"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_songs_by_spotify_id_songs_spotify_get() {
    local body="${1:-{}}"
    api_request GET "/songs/spotify" -d "$body" | pretty
}

get_users_users_get() {
    local query="${1:-}"
    local path="/users"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_test_user_users_post() {
    api_request POST "/users" | pretty
}

list_api_keys_users_me_api_keys_get() {
    local query="${1:-}"
    local path="/users/me/api-keys"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

create_api_key_users_me_api_keys_post() {
    local body="${1:-{}}"
    api_request POST "/users/me/api-keys" -d "$body" | pretty
}

delete_api_key_users_me_api_keys_api_key_id_delete() {
    local api_key_id="${1:?"api_key_id is required"}"
    api_request DELETE "/users/me/api-keys/$api_key_id" | pretty
}

get_notifications_users_me_notifications_get() {
    local query="${1:-}"
    local path="/users/me/notifications"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_preferences_users_me_preferences_get() {
    local query="${1:-}"
    local path="/users/me/preferences"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

update_preferences_users_me_preferences_put() {
    local body="${1:-{}}"
    api_request PUT "/users/me/preferences" -d "$body" | pretty
}

clear_primary_artist_users_me_primary_artist_delete() {
    api_request DELETE "/users/me/primary-artist" | pretty
}

set_primary_artist_users_me_primary_artist_put() {
    local body="${1:-{}}"
    api_request PUT "/users/me/primary-artist" -d "$body" | pretty
}

get_user_note_collections_users_created_by_id_notes_collections_get() {
    local created_by_id="${1:?"created_by_id is required"}"
    local query="${2:-}"
    local path="/users/$created_by_id/notes/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_recording_collections_users_created_by_id_recordings_collections_get() {
    local created_by_id="${1:?"created_by_id is required"}"
    local query="${2:-}"
    local path="/users/$created_by_id/recordings/collections"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

add_user_link_users_logged_in_user_id_user_links_post() {
    local logged_in_user_id="${1:?"logged_in_user_id is required"}"
    api_request POST "/users/$logged_in_user_id/user_links" | pretty
}

delete_user_link_users_logged_in_user_id_user_links_link_to_delete_id_delete() {
    local logged_in_user_id="${1:?"logged_in_user_id is required"}"
    local link_to_delete_id="${2:?"link_to_delete_id is required"}"
    api_request DELETE "/users/$logged_in_user_id/user_links/$link_to_delete_id" | pretty
}

get_user_users_user_id_get() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_events_users_user_id_events_get() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_notes_users_user_id_notes_get() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id/notes"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_user_recordings_users_user_id_recordings_get() {
    local user_id="${1:?"user_id is required"}"
    local query="${2:-}"
    local path="/users/$user_id/recordings"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

update_user_users_user_to_change_id_put() {
    local user_to_change_id="${1:?"user_to_change_id is required"}"
    local body="${2:-{}}"
    api_request PUT "/users/$user_to_change_id" -d "$body" | pretty
}

get_profile_image_upload_url_users_user_to_change_id_image_upload_url_post() {
    local user_to_change_id="${1:?"user_to_change_id is required"}"
    api_request POST "/users/$user_to_change_id/image/upload-url" | pretty
}

create_venue_venues_post() {
    local body="${1:-{}}"
    api_request POST "/venues" -d "$body" | pretty
}

get_venue_venues_venue_id_get() {
    local venue_id="${1:?"venue_id is required"}"
    local query="${2:-}"
    local path="/venues/$venue_id"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

get_venue_events_venues_venue_id_events_get() {
    local venue_id="${1:?"venue_id is required"}"
    local query="${2:-}"
    local path="/venues/$venue_id/events"
    if [[ -n "$query" ]]; then
        path="${path}?${query}"
    fi
    api_request GET "$path" | pretty
}

show_help() {
    cat <<HELP
stardust - Stardust Jams CLI

Usage: stardust <command> [args...]

Commands:
  get-status-get                                                                                 Get Status
  get-status-status-get                                                                          Get Status
  create-artist-artists-post [json-body]                                                         Create Artist
  get-artist-artists-artist-id-get <artist_id>                                                   Get Artist
  get-artist-events-artists-artist-id-events-get <artist_id>                                     Get Artist Events
  request-membership-artists-artist-id-members-post <artist_id>                                  Request Membership
  remove-own-membership-artists-artist-id-members-me-delete <artist_id>                          Remove Own Membership
  approve-membership-artists-artist-id-members-member-user-id-approve-post <artist_id> <member_user_id>  Approve Membership
  reject-membership-artists-artist-id-members-member-user-id-reject-post <artist_id> <member_user_id>  Reject Membership
  get-artist-notes-artists-artist-id-notes-get <artist_id>                                       Get Artist Notes
  get-artist-recordings-artists-artist-id-recordings-get <artist_id>                             Get Artist Recordings
  auth-apple-auth-apple-post [json-body]                                                         Auth Apple
  auth-refresh-auth-apple-refresh-post [json-body]                                               Auth Refresh
  auth-refresh-auth-refresh-post [json-body]                                                     Auth Refresh
  auth-sign-out-auth-sign-out-post [json-body]                                                   Auth Sign Out
  get-content-url-content-content-type-content-id-get <content_type> <content_id>                Get Content Url
  get-events-by-id-events-get                                                                    Get Events By Id
  create-event-events-post [json-body]                                                           Create Event
  check-event-statuses-events-sync-post [json-body]                                              Check Event Statuses
  get-event-by-uuid-events-uuid-uuid-get <uuid>                                                  Get Event By Uuid
  post-event-users-events-code-users-get <code>                                                  Post Event Users
  get-event-events-event-id-get <event_id>                                                       Get Event
  update-event-events-event-id-put <event_id> [json-body]                                        Update Event
  like-event-events-event-id-like-post <event_id>                                                Like Event
  set-rsvp-status-events-event-id-rsvp-post <event_id> [json-body]                               Set Rsvp Status
  get-set-list-events-event-id-setlist-get <event_id>                                            Get Set List
  add-to-set-list-events-event-id-setlist-post <event_id> [json-body]                            Add To Set List
  reorder-set-list-events-event-id-setlist-reorder-put <event_id> [json-body]                    Reorder Set List
  remove-from-set-list-events-event-id-setlist-note-id-delete <event_id> <note_id>               Remove From Set List
  add-event-songs-events-event-id-songs-get <event_id>                                           Add Event Songs
  unlike-event-events-event-id-unlike-post <event_id>                                            Unlike Event
  get-locations-locations-get                                                                    Get Locations
  get-location-locations-location-id-get <location_id>                                           Get Location
  get-map-events-map-events-get                                                                  Get Map Events
  get-note-tags-note-tags-get                                                                    Get Note Tags
  create-note-tag-note-tags-post [json-body]                                                     Create Note Tag
  delete-note-tag-note-tags-note-tag-id-delete <note_tag_id>                                     Delete Note Tag
  update-note-tag-note-tags-note-tag-id-put <note_tag_id> [json-body]                            Update Note Tag
  get-notes-by-id-notes-get                                                                      Get Notes By Id
  upload-note-notes-post [json-body]                                                             Upload Note
  get-notes-by-note-tag-notes-by-note-tag-note-tag-id-get <note_tag_id>                          Get Notes By Note Tag
  get-note-collections-by-id-notes-collections-get                                               Get Note Collections By Id
  get-suggested-tips-notes-suggested-tips-post [json-body]                                       Get Suggested Tips
  check-note-statuses-notes-sync-post [json-body]                                                Check Note Statuses
  get-note-by-uuid-notes-uuid-uuid-get <uuid>                                                    Get Note By Uuid
  get-tags-for-note-notes-note-id-note-tags-get <note_id>                                        Get Tags For Note
  set-tags-for-note-notes-note-id-note-tags-put <note_id> [json-body]                            Set Tags For Note
  remove-tag-from-note-notes-note-id-note-tags-note-tag-id-delete <note_id> <note_tag_id>        Remove Tag From Note
  create-post-posts-post [json-body]                                                             Create Post
  like-comment-posts-comments-comment-id-like-post <comment_id>                                  Like Comment
  unlike-comment-posts-comments-comment-id-unlike-post <comment_id>                              Unlike Comment
  get-post-comments-posts-post-id-comments-get <post_id>                                         Get Post Comments
  create-comment-posts-post-id-comments-post <post_id> [json-body]                               Create Comment
  like-post-posts-post-id-like-post <post_id>                                                    Like Post
  unlike-post-posts-post-id-unlike-post <post_id>                                                Unlike Post
  get-purposeful-debug-log-purposeful-debug-log-get                                              Get Purposeful Debug Log
  get-purposeful-error-purposeful-error-get                                                      Get Purposeful Error
  get-purposeful-error-log-purposeful-error-log-get                                              Get Purposeful Error Log
  get-purposeful-info-log-purposeful-info-log-get                                                Get Purposeful Info Log
  get-purposeful-warning-log-purposeful-warning-log-get                                          Get Purposeful Warning Log
  get-recordings-by-id-recordings-get                                                            Get Recordings By Id
  upload-recording-recordings-post [json-body]                                                   Upload Recording
  get-recording-collections-by-id-recordings-collections-get                                     Get Recording Collections By Id
  check-recording-statuses-recordings-sync-post [json-body]                                      Check Recording Statuses
  get-recording-by-uuid-recordings-uuid-uuid-get <uuid>                                          Get Recording By Uuid
  get-recording-recordings-recording-id-get <recording_id>                                       Get Recording
  like-recording-recordings-recording-id-like-post <recording_id>                                Like Recording
  unlike-recording-recordings-recording-id-unlike-post <recording_id>                            Unlike Recording
  register-device-register-device-post [json-body]                                               Register Device
  get-search-search-get                                                                          Get Search
  get-songs-by-id-songs-get                                                                      Get Songs By Id
  get-songs-by-spotify-id-songs-spotify-get [json-body]                                          Get Songs By Spotify Id
  get-users-users-get                                                                            Get Users
  create-test-user-users-post                                                                    Create Test User
  list-api-keys-users-me-api-keys-get                                                            List Api Keys
  create-api-key-users-me-api-keys-post [json-body]                                              Create Api Key
  delete-api-key-users-me-api-keys-api-key-id-delete <api_key_id>                                Delete Api Key
  get-notifications-users-me-notifications-get                                                   Get Notifications
  get-preferences-users-me-preferences-get                                                       Get Preferences
  update-preferences-users-me-preferences-put [json-body]                                        Update Preferences
  clear-primary-artist-users-me-primary-artist-delete                                            Clear Primary Artist
  set-primary-artist-users-me-primary-artist-put [json-body]                                     Set Primary Artist
  get-user-note-collections-users-created-by-id-notes-collections-get <created_by_id>            Get User Note Collections
  get-user-recording-collections-users-created-by-id-recordings-collections-get <created_by_id>  Get User Recording Collections
  add-user-link-users-logged-in-user-id-user-links-post <logged_in_user_id>                      Add User Link
  delete-user-link-users-logged-in-user-id-user-links-link-to-delete-id-delete <logged_in_user_id> <link_to_delete_id>  Delete User Link
  get-user-users-user-id-get <user_id>                                                           Get User
  get-user-events-users-user-id-events-get <user_id>                                             Get User Events
  get-user-notes-users-user-id-notes-get <user_id>                                               Get User Notes
  get-user-recordings-users-user-id-recordings-get <user_id>                                     Get User Recordings
  update-user-users-user-to-change-id-put <user_to_change_id> [json-body]                        Update User
  get-profile-image-upload-url-users-user-to-change-id-image-upload-url-post <user_to_change_id>  Get Profile Image Upload Url
  create-venue-venues-post [json-body]                                                           Create Venue
  get-venue-venues-venue-id-get <venue_id>                                                       Get Venue
  get-venue-events-venues-venue-id-events-get <venue_id>                                         Get Venue Events

Environment:
  STARDUST_API_KEY    API key for authentication (required)
  STARDUST_API_URL    API server URL (default: https://stardust-jams.stardustlabs.io)
HELP
}

command="${1:-help}"
shift || true

case "$command" in
    get-status-get) get_status_get "$@" ;;
    get-status-status-get) get_status_status_get "$@" ;;
    create-artist-artists-post) create_artist_artists_post "$@" ;;
    get-artist-artists-artist-id-get) get_artist_artists_artist_id_get "$@" ;;
    get-artist-events-artists-artist-id-events-get) get_artist_events_artists_artist_id_events_get "$@" ;;
    request-membership-artists-artist-id-members-post) request_membership_artists_artist_id_members_post "$@" ;;
    remove-own-membership-artists-artist-id-members-me-delete) remove_own_membership_artists_artist_id_members_me_delete "$@" ;;
    approve-membership-artists-artist-id-members-member-user-id-approve-post) approve_membership_artists_artist_id_members_member_user_id_approve_post "$@" ;;
    reject-membership-artists-artist-id-members-member-user-id-reject-post) reject_membership_artists_artist_id_members_member_user_id_reject_post "$@" ;;
    get-artist-notes-artists-artist-id-notes-get) get_artist_notes_artists_artist_id_notes_get "$@" ;;
    get-artist-recordings-artists-artist-id-recordings-get) get_artist_recordings_artists_artist_id_recordings_get "$@" ;;
    auth-apple-auth-apple-post) auth_apple_auth_apple_post "$@" ;;
    auth-refresh-auth-apple-refresh-post) auth_refresh_auth_apple_refresh_post "$@" ;;
    auth-refresh-auth-refresh-post) auth_refresh_auth_refresh_post "$@" ;;
    auth-sign-out-auth-sign-out-post) auth_sign_out_auth_sign_out_post "$@" ;;
    get-content-url-content-content-type-content-id-get) get_content_url_content_content_type_content_id_get "$@" ;;
    get-events-by-id-events-get) get_events_by_id_events_get "$@" ;;
    create-event-events-post) create_event_events_post "$@" ;;
    check-event-statuses-events-sync-post) check_event_statuses_events_sync_post "$@" ;;
    get-event-by-uuid-events-uuid-uuid-get) get_event_by_uuid_events_uuid_uuid_get "$@" ;;
    post-event-users-events-code-users-get) post_event_users_events_code_users_get "$@" ;;
    get-event-events-event-id-get) get_event_events_event_id_get "$@" ;;
    update-event-events-event-id-put) update_event_events_event_id_put "$@" ;;
    like-event-events-event-id-like-post) like_event_events_event_id_like_post "$@" ;;
    set-rsvp-status-events-event-id-rsvp-post) set_rsvp_status_events_event_id_rsvp_post "$@" ;;
    get-set-list-events-event-id-setlist-get) get_set_list_events_event_id_setlist_get "$@" ;;
    add-to-set-list-events-event-id-setlist-post) add_to_set_list_events_event_id_setlist_post "$@" ;;
    reorder-set-list-events-event-id-setlist-reorder-put) reorder_set_list_events_event_id_setlist_reorder_put "$@" ;;
    remove-from-set-list-events-event-id-setlist-note-id-delete) remove_from_set_list_events_event_id_setlist_note_id_delete "$@" ;;
    add-event-songs-events-event-id-songs-get) add_event_songs_events_event_id_songs_get "$@" ;;
    unlike-event-events-event-id-unlike-post) unlike_event_events_event_id_unlike_post "$@" ;;
    get-locations-locations-get) get_locations_locations_get "$@" ;;
    get-location-locations-location-id-get) get_location_locations_location_id_get "$@" ;;
    get-map-events-map-events-get) get_map_events_map_events_get "$@" ;;
    get-note-tags-note-tags-get) get_note_tags_note_tags_get "$@" ;;
    create-note-tag-note-tags-post) create_note_tag_note_tags_post "$@" ;;
    delete-note-tag-note-tags-note-tag-id-delete) delete_note_tag_note_tags_note_tag_id_delete "$@" ;;
    update-note-tag-note-tags-note-tag-id-put) update_note_tag_note_tags_note_tag_id_put "$@" ;;
    get-notes-by-id-notes-get) get_notes_by_id_notes_get "$@" ;;
    upload-note-notes-post) upload_note_notes_post "$@" ;;
    get-notes-by-note-tag-notes-by-note-tag-note-tag-id-get) get_notes_by_note_tag_notes_by_note_tag_note_tag_id_get "$@" ;;
    get-note-collections-by-id-notes-collections-get) get_note_collections_by_id_notes_collections_get "$@" ;;
    get-suggested-tips-notes-suggested-tips-post) get_suggested_tips_notes_suggested_tips_post "$@" ;;
    check-note-statuses-notes-sync-post) check_note_statuses_notes_sync_post "$@" ;;
    get-note-by-uuid-notes-uuid-uuid-get) get_note_by_uuid_notes_uuid_uuid_get "$@" ;;
    get-tags-for-note-notes-note-id-note-tags-get) get_tags_for_note_notes_note_id_note_tags_get "$@" ;;
    set-tags-for-note-notes-note-id-note-tags-put) set_tags_for_note_notes_note_id_note_tags_put "$@" ;;
    remove-tag-from-note-notes-note-id-note-tags-note-tag-id-delete) remove_tag_from_note_notes_note_id_note_tags_note_tag_id_delete "$@" ;;
    create-post-posts-post) create_post_posts_post "$@" ;;
    like-comment-posts-comments-comment-id-like-post) like_comment_posts_comments_comment_id_like_post "$@" ;;
    unlike-comment-posts-comments-comment-id-unlike-post) unlike_comment_posts_comments_comment_id_unlike_post "$@" ;;
    get-post-comments-posts-post-id-comments-get) get_post_comments_posts_post_id_comments_get "$@" ;;
    create-comment-posts-post-id-comments-post) create_comment_posts_post_id_comments_post "$@" ;;
    like-post-posts-post-id-like-post) like_post_posts_post_id_like_post "$@" ;;
    unlike-post-posts-post-id-unlike-post) unlike_post_posts_post_id_unlike_post "$@" ;;
    get-purposeful-debug-log-purposeful-debug-log-get) get_purposeful_debug_log_purposeful_debug_log_get "$@" ;;
    get-purposeful-error-purposeful-error-get) get_purposeful_error_purposeful_error_get "$@" ;;
    get-purposeful-error-log-purposeful-error-log-get) get_purposeful_error_log_purposeful_error_log_get "$@" ;;
    get-purposeful-info-log-purposeful-info-log-get) get_purposeful_info_log_purposeful_info_log_get "$@" ;;
    get-purposeful-warning-log-purposeful-warning-log-get) get_purposeful_warning_log_purposeful_warning_log_get "$@" ;;
    get-recordings-by-id-recordings-get) get_recordings_by_id_recordings_get "$@" ;;
    upload-recording-recordings-post) upload_recording_recordings_post "$@" ;;
    get-recording-collections-by-id-recordings-collections-get) get_recording_collections_by_id_recordings_collections_get "$@" ;;
    check-recording-statuses-recordings-sync-post) check_recording_statuses_recordings_sync_post "$@" ;;
    get-recording-by-uuid-recordings-uuid-uuid-get) get_recording_by_uuid_recordings_uuid_uuid_get "$@" ;;
    get-recording-recordings-recording-id-get) get_recording_recordings_recording_id_get "$@" ;;
    like-recording-recordings-recording-id-like-post) like_recording_recordings_recording_id_like_post "$@" ;;
    unlike-recording-recordings-recording-id-unlike-post) unlike_recording_recordings_recording_id_unlike_post "$@" ;;
    register-device-register-device-post) register_device_register_device_post "$@" ;;
    get-search-search-get) get_search_search_get "$@" ;;
    get-songs-by-id-songs-get) get_songs_by_id_songs_get "$@" ;;
    get-songs-by-spotify-id-songs-spotify-get) get_songs_by_spotify_id_songs_spotify_get "$@" ;;
    get-users-users-get) get_users_users_get "$@" ;;
    create-test-user-users-post) create_test_user_users_post "$@" ;;
    list-api-keys-users-me-api-keys-get) list_api_keys_users_me_api_keys_get "$@" ;;
    create-api-key-users-me-api-keys-post) create_api_key_users_me_api_keys_post "$@" ;;
    delete-api-key-users-me-api-keys-api-key-id-delete) delete_api_key_users_me_api_keys_api_key_id_delete "$@" ;;
    get-notifications-users-me-notifications-get) get_notifications_users_me_notifications_get "$@" ;;
    get-preferences-users-me-preferences-get) get_preferences_users_me_preferences_get "$@" ;;
    update-preferences-users-me-preferences-put) update_preferences_users_me_preferences_put "$@" ;;
    clear-primary-artist-users-me-primary-artist-delete) clear_primary_artist_users_me_primary_artist_delete "$@" ;;
    set-primary-artist-users-me-primary-artist-put) set_primary_artist_users_me_primary_artist_put "$@" ;;
    get-user-note-collections-users-created-by-id-notes-collections-get) get_user_note_collections_users_created_by_id_notes_collections_get "$@" ;;
    get-user-recording-collections-users-created-by-id-recordings-collections-get) get_user_recording_collections_users_created_by_id_recordings_collections_get "$@" ;;
    add-user-link-users-logged-in-user-id-user-links-post) add_user_link_users_logged_in_user_id_user_links_post "$@" ;;
    delete-user-link-users-logged-in-user-id-user-links-link-to-delete-id-delete) delete_user_link_users_logged_in_user_id_user_links_link_to_delete_id_delete "$@" ;;
    get-user-users-user-id-get) get_user_users_user_id_get "$@" ;;
    get-user-events-users-user-id-events-get) get_user_events_users_user_id_events_get "$@" ;;
    get-user-notes-users-user-id-notes-get) get_user_notes_users_user_id_notes_get "$@" ;;
    get-user-recordings-users-user-id-recordings-get) get_user_recordings_users_user_id_recordings_get "$@" ;;
    update-user-users-user-to-change-id-put) update_user_users_user_to_change_id_put "$@" ;;
    get-profile-image-upload-url-users-user-to-change-id-image-upload-url-post) get_profile_image_upload_url_users_user_to_change_id_image_upload_url_post "$@" ;;
    create-venue-venues-post) create_venue_venues_post "$@" ;;
    get-venue-venues-venue-id-get) get_venue_venues_venue_id_get "$@" ;;
    get-venue-events-venues-venue-id-events-get) get_venue_events_venues_venue_id_events_get "$@" ;;
    help|--help|-h) show_help ;;
    *)
        echo "Unknown command: $command" >&2
        echo "Run 'stardust help' for usage." >&2
        exit 1
        ;;
esac
